spring:
  config:
    activate:
      on-profile: preproduccion
  datasource:
    url: jdbc:postgresql://db.cljvpenzjwrezehdsdul.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: WAqYUsVPaOY6qj5g
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

logging:
  level:
    com.zms.gohostdeliveryservice: INFO
    org.springframework.kafka: WARN
